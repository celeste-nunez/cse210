class Member : User
{
    public override void GetUserSelections()
    {
        MemberMenu();
        int userSelection = int.Parse(Console.ReadLine());
        if (userSelection == 1) 
        {
            
        }

    }
}